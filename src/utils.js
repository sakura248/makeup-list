// const includesWord = (target, search) => {
//   return target.toUpperCase().includes(search.toUpperCase());
// };

// export const filterWith = (list, target, search) => {
//   let tempList = []
//   if (search.length > 0) {
//     tempList = list.
//   }
// }
